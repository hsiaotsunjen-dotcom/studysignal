"use client";

import Volume2Icon from "lucide-react/dist/esm/icons/volume-2.js";

/** Single deep import of Lucide Volume2 — avoids webpack issues when mixing with `optimizePackageImports` barrel graph. */
export const Volume2 = Volume2Icon;
